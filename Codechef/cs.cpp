#include<bits/stdc++.h>
using namespace std;
#include<algorithm>
#include<vector>
#include<map>
#define repVector(v)  for( typeof(v.begin()) it = v.begin(); it != v.end(); it++ )


int main()
{
	int n,m;
	cin>>n>>m
	
	vector<int> i,a,v,p;
	int id,attr,val,pri;
	 for(int i=0;i<n;i++)
	 {
	 	cin>>id>>attr>>val>>pri;
	 	i.push_back(id);
	 	a.push_back(attr);
	 	v.push_back(val);
	 	p.push_back(pri);
	 }
	 
	
	
	
}
