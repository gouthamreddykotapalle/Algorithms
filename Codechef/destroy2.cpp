#include<bits/stdc++.h>
using namespace std;
#include<vector>
#define get(a) scanf("%lld",&a)
#define ll long long
#define FOR(i,a,b) for(ll i=a;i<b;i++)
//#define BOOST ios_base::sync_with_stdio(false); cin.tie(NULL)

int main()
{
	ll n,m,i,t,count,a;
	get(t);
	vector<ll> v;
	//BOOST;
	
	while(t--)
	{
		get(n);
		count=0;
	    for(a =0;a<n;a++)//1 2 3 3 3 4 5 6
	    {
	    	cin>>i;
	    	v[i]++;//
	    }
	   
	   
		
	}
	
	
	
	
}
