#include<bits/stdc++.h>
using namespace std;
#include<vector>
#define get(a) scanf("%lld",&a)
#define ll long long
#define pb push_back
#include<string>
#define FOR(i,a,b) for(ll i=a;i<b;i++)
#define BOOST ios_base::sync_with_stdio(false); cin.tie(NULL)


int main()
{
	
	ll i,t,n,c,p,d,min,f;
strint s,p;
    cin>>s;
	cin>>p;
	k=0;
	for(i=0;i<s.length();i++)
	{
	 
	 for(j=k;j<p.length();j++)
	 {
	 	if(a[i]==a[j])
	 	{
	 		count++;
	 		break;
	 		
	 	}
	 	
	 }
	 k=j;
	 
	}	 
}
